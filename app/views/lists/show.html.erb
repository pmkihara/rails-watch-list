<div class="banner banner-section" style="background-image: linear-gradient(0deg, rgba(0,0,0,0.75) 25%, rgba(0,0,0,0.7) 100%), url('<%= image_url 'movies.jpg'%>')">
  <div class="banner-content">
    <div class="banner-container p-2 p-md-3">
      <h1 id="listname"><%= @list.name %></h1>
      <%= link_to edit_list_path(@list), class: "mx-3", data: { turbo: true, turbo_frame: "modal" } do %>
        <div class="btn-capsule-sm btn-frosted">
          <i class="fas fa-pen me-2"></i> Edit
        </div>
      <% end %>
      <%= link_to list_path(@list), method: :delete, class: "mx-3", data: { confirm: "Are you sure you want to delete this watchlist?" } do %>
        <div class="btn-capsule-sm btn-frosted">
          <i class="far fa-trash-alt me-2"></i> Delete
        </div>
      <% end %>
    </div>
  </div>
</div>
<div class="container p-1 p-sm-3 text-center">
  <%= link_to new_list_bookmark_path(list_id: @list), class: "btn-capsule btn-gold my-3", data: { turbo: true, turbo_frame: 'modal' } do %>
    <i class="fas fa-plus me-2"></i>Add a movie
  <% end %>
  <div class="cards p-1 p-sm-3" id="movie-cards">
    <% @list.bookmarks&.each do |bookmark| %>
      <%= render "bookmarks/bookmark", bookmark: bookmark %>
    <% end %>
  </div>
</div>
