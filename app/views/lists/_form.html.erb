<%= simple_form_for list, html: { class: "mt-3 text-center", data: { turbo: true } } do |f| %>
  <%= f.input :name, label: false, placeholder: "Enter the name of your new list",  wrapper_html: {class: "form-list"} %>
  <%= f.submit class: "btn-capsule-sm btn-outline-gold mt-2" %>
<% end %>
